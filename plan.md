# Leo Team Blog 开发计划

## 1. 项目目标

Leo Team Blog 是一个团队博客网站，用于展示技术学习笔记、项目实践、团队协作过程和阶段性成长记录。

第一阶段的目标是完成一个基于 Vue 3 + Vite 的静态前端网站。网站应具备清晰的内容结构、现代简洁的视觉风格、良好的响应式体验，并为后续扩展文章详情、项目展示、团队介绍等页面留下稳定架构。

项目初期不引入复杂后端系统。博客文章、项目数据和团队成员信息可以先通过本地 Markdown 或 JavaScript 数据文件维护，等前端体验稳定后再考虑接入后端或 CMS。

## 2. 产品方向

网站主要包含以下模块：

- 首页：首屏欢迎区、精选文章、项目亮点、团队入口。
- 博客：文章列表、分类或标签筛选、文章详情。
- 项目：团队项目卡片、技术栈、项目状态和相关链接。
- 团队：成员介绍、角色分工、技能方向和协作说明。
- 关于：网站定位、团队背景、联系方式和课程/作业说明。

视觉方向参考已有的 `设计.md`：

- 以黑、白、浅灰为基础色。
- 使用少量青绿色作为强调色。
- 首页首屏使用大字号标题。
- 背景使用代码生成的点阵或粒子效果。
- 桌面端保留轻量鼠标交互。
- 移动端降低动效复杂度，优先保证阅读体验。

## 3. 技术栈

- 前端框架：Vue 3
- 构建工具：Vite
- 开发语言：优先使用 JavaScript，团队熟悉后可逐步引入 TypeScript
- 路由管理：Vue Router
- 状态管理：仅在确实需要跨页面共享状态时使用 Pinia
- 内容管理：第一阶段使用本地 Markdown 或本地数据文件
- 样式方案：全局 CSS 变量 + 组件局部样式
- 测试方向：后续为工具函数、核心组件和主要页面补充测试

## 4. 架构原则

项目目录按职责划分：

- `src/views/`：页面级视图，例如首页、博客页、项目页、团队页。
- `src/components/`：可复用组件。
- `src/components/layout/`：导航栏、页脚、页面外壳等布局组件。
- `src/components/home/`：首页专属区块组件。
- `src/components/blog/`：文章卡片、标签、文章列表和文章渲染相关组件。
- `src/components/projects/`：项目展示相关组件。
- `src/components/team/`：团队成员展示相关组件。
- `src/content/posts/`：博客文章内容。
- `src/data/`：导航、文章摘要、项目、成员等结构化数据。
- `src/router/`：路由配置。
- `src/services/`：内容读取、数据整理等服务逻辑。
- `src/composables/`：可复用组合式逻辑，例如鼠标位置、响应式状态、动效开关。
- `src/styles/`：样式变量、重置样式、全局样式、布局工具类和通用动画。
- `src/utils/`：与 Vue 无关的通用工具函数。
- `src/types/`：如果后续引入 TypeScript，用于存放共享类型定义。

每个文件都应有明确职责。页面变复杂后，应及时拆分组件，避免一个文件承担过多内容。

## 5. 开发阶段

### 阶段一：项目基础

目标：让项目可以正常运行，并建立基础开发规范。

任务：

- 创建 Vue 3 + Vite 项目基础文件。
- 添加开发、构建、预览等 npm scripts。
- 创建 `App.vue`、`main.js`、路由入口和基础样式文件。
- 定义颜色、间距、字体、阴影等 CSS 变量。
- 实现基础页面布局，包括导航栏和页脚。
- 检查桌面端和移动端是否存在内容溢出。

完成标准：

- `npm run dev` 可以启动项目。
- 首页路由可以正常渲染。
- 基础响应式布局可用。

### 阶段二：首页首屏

目标：实现 `设计.md` 中描述的首页核心视觉体验。

任务：

- 实现响应式导航栏。
- 实现 `HeroSection`，包含主标题、副标题、按钮和滚动提示。
- 实现 `ParticleBackground`，先完成静态点阵背景。
- 增加桌面端标题跟随鼠标的轻微位移效果。
- 增加桌面端圆形遮罩文字切换效果。
- 移动端关闭复杂鼠标交互。
- 在首屏下方增加文章、项目、团队的预览入口。

完成标准：

- 首页风格与设计方向一致。
- 移动端标题和按钮清晰可读。
- 动效不造成明显卡顿或布局抖动。

### 阶段三：博客内容

目标：让网站具备真实博客的基本阅读能力。

任务：

- 定义文章数据结构。
- 在 `src/content/posts/` 中添加示例文章。
- 实现文章列表页。
- 实现文章卡片组件。
- 实现标签或分类筛选。
- 实现文章详情页。
- 处理空内容和不存在文章的情况。

完成标准：

- 用户可以浏览文章列表。
- 用户可以进入文章详情页。
- 博客页面保持清晰、稳定、易读。

### 阶段四：项目与团队

目标：完整展示团队项目和成员信息。

任务：

- 在 `src/data/` 中定义项目数据。
- 实现项目页和项目卡片组件。
- 在 `src/data/` 中定义团队成员数据。
- 实现团队页和成员卡片组件。
- 从首页预览区跳转到完整页面。

完成标准：

- 项目页能清楚说明团队做过什么。
- 团队页能清楚说明成员是谁、负责什么。

### 阶段五：打磨与交付

目标：让项目达到课程展示或作业提交质量。

任务：

- 优化间距、字体、悬停状态和焦点状态。
- 检查移动端布局。
- 补充语义化 HTML 和可访问性信息。
- 添加浏览器标题和页面描述。
- 执行生产构建。
- 修复控制台错误和路由问题。
- 完善 README，说明运行方式和项目结构。

完成标准：

- 生产构建成功。
- 主要页面和导航可用。
- 网站可以稳定演示。

## 6. 编码规范

- 组件命名要清晰，例如 `HeroSection.vue`、`ArticleCard.vue`、`TeamMemberCard.vue`。
- 页面组件负责组织页面结构，不承担过多底层 UI 细节。
- 可复用基础组件放在 `components/base/`。
- 优先使用 CSS 变量，避免在各处重复硬编码颜色。
- class 命名保持直观，不使用难懂缩写。
- 动效保持克制，并优先保证阅读体验。
- 修改桌面端布局时，必须同时检查移动端。
- 前端静态体验完成前，不急于引入后端。

## 7. 测试与验证

初期可以先进行人工检查，但每个阶段都要有明确验证项：

- 首页可以正常加载。
- 导航链接可以跳转。
- 移动端宽度下不出现横向溢出。
- 文章列表可以从数据中渲染。
- 文章详情可以处理不存在的文章。
- 生产构建可以成功执行。

后续可逐步增加自动化测试：

- 为数据筛选、格式化等工具函数添加单元测试。
- 为文章卡片、导航栏等核心组件添加组件测试。
- 为首页、博客列表、文章详情、项目页、团队页添加端到端测试。

## 8. 风险控制

- 避免过早做复杂系统，第一版以静态前端为主。
- 动效不能影响内容阅读。
- 页面文件过大时及时拆分组件。
- 文章、项目、成员数据要从一开始就有清晰位置。
- 移动端必须有降级方案，不依赖鼠标交互。

## 9. 下一步建议

下一步应创建可运行的 Vue 3 + Vite 项目基础，然后实现首页外壳。建议第一个开发提交只包含项目启动文件、基础布局、全局样式和路由骨架。
